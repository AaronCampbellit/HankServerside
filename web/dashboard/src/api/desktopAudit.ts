import {apiClient,type ApiTransport} from "./client";
export interface DesktopAuditEvent {session_id:string;sequence:number;event_type:string;actor_type:string;occurred_at:string;severity:string;reason_code:string;metadata:Record<string,string>}
export interface DesktopSessionDetail {session_id:string;state:string;key_epoch:number;hard_expires_at:string;reconnect_expires_at?:string|null;permissions:string[];termination_reason?:string;readiness:Record<string,boolean|string>;active_session:{active:boolean;state:string;key_epoch:number;hard_expires_at:string;reconnect_expires_at?:string|null}}
export interface DesktopAgentReadiness {agent_id:string;online:boolean;platform:string;trusted:boolean;checks:Record<string,string>;capabilities:string[];active_session:boolean;reported_at?:string|null}
export interface DesktopTerminalSession {session_id:string;agent_id:string;operator_user_id:string;state:string;termination_reason:string;platform:string;requested_at:string;active_at?:string|null;terminated_at?:string|null;duration_ms:number;epoch_count:number;browser_to_agent_bytes:number;agent_to_browser_bytes:number;permissions:string[]}
export class DesktopAuditClient {constructor(private readonly api:ApiTransport=apiClient){} detail(id:string):Promise<DesktopSessionDetail>{return this.api.request(`/v1/desktop-sessions/${encodeURIComponent(id)}`)} events(id:string,after=0,limit=50):Promise<{events:DesktopAuditEvent[];next_after_sequence:number}>{return this.api.request(`/v1/desktop-sessions/${encodeURIComponent(id)}/events?after_sequence=${after}&limit=${Math.min(100,Math.max(1,limit))}`)} readiness(agentID:string):Promise<DesktopAgentReadiness>{return this.api.request(`/v1/agents/${encodeURIComponent(agentID)}/desktop-readiness`)} history(after=0,limit=25):Promise<{sessions:DesktopTerminalSession[];next_after:number}>{return this.api.request(`/v1/home/desktop-sessions?after=${Math.max(0,after)}&limit=${Math.min(100,Math.max(1,limit))}`)}}
export const desktopAuditClient=new DesktopAuditClient();
